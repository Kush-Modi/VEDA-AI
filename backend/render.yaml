services:
  - type: web
    name: vedaai-backend
    rootDir: backend
    env: node
    buildCommand: npm install && npx tsc
    startCommand: node dist/server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
